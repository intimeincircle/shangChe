// Generated by ScalaTS 0.5.7: https://scala-ts.github.io/scala-ts/

import { DateTime, isDateTime } from './DateTime';

export interface ShameStockPState {
  price: number;
  name?: string;
  currentPrice?: number;
  priceUpdateAt?: DateTime;
  chaodie?: number;
}

export function isShameStockPState(v: any): v is ShameStockPState {
  return (
    (!v['chaodie'] || ((typeof v['chaodie']) === 'number')) &&
    (!v['priceUpdateAt'] || (v['priceUpdateAt'] && isDateTime(v['priceUpdateAt']))) &&
    (!v['currentPrice'] || ((typeof v['currentPrice']) === 'number')) &&
    (!v['name'] || ((typeof v['name']) === 'string')) &&
    ((typeof v['price']) === 'number')
  );
}
